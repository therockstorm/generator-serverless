{
  "name": "<%= serviceName %>",
  "version": "1.0.0",
  "scripts": {
    "build": "sbt test assembly",
    "deploy": "npm run build && serverless deploy --verbose",
    "deploy:docker": "npm run build && node ./node_modules/.bin/docker-cli publish <%= dockerNamespace %>/${npm_package_name} --tag $(git rev-parse HEAD)",
    "verify": "npm run deploy -- --noDeploy"
  },
  "devDependencies": {
    "@therockstorm/docker-cli": "^1.0.6",
    "serverless": "^1.28.0"
  },
  "private": true
}
