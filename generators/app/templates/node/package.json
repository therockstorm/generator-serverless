{
  "name": "<%= serviceName %>",
  "version": "1.0.0",
  "scripts": {
    "build": "serverless webpack",
    "build:docker": "npm run build && cp Dockerfile .dockerignore dist/service",
    "clean": "rm -rf dist",
    "precommit": "lint-staged",
    "deploy": "npm test && serverless deploy --verbose",
    "deploy:docker": "npm run test && npm run build:docker && cd dist/service && node ../../node_modules/.bin/docker-cli publish <%= dockerNamespace %>/${npm_package_name} --tag $(git rev-parse HEAD)",
    "format": "prettier --write '{,!(dist)/**/}*.js'",
    "lint": "standard --fix",
    "start": "npm run build && cd dist/service && node server.js",
    "pretest": "npm run format && npm run lint",
    "test": "ENVIRONMENT=test mocha --require @babel/register",
    "verify": "npm run deploy -- --noDeploy",
    "watch": "npm test -- --watch"
  },
  "dependencies": {
    "@therockstorm/utils": "^1.0.7"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0-beta.47",
    "@babel/preset-env": "^7.0.0-beta.47",
    "@babel/register": "^7.0.0-beta.47",
    "@therockstorm/docker-cli": "^1.0.6",
    "babel-loader": "^8.0.0-beta.3",
    "husky": "^0.14.3",
    "lint-staged": "^7.1.2",
    "mocha": "^5.2.0",
    "prettier": "^1.12.1",
    "serverless": "^1.27.2",
    "serverless-webpack": "^5.1.5",
    "standard": "^11.0.1",
    "webpack": "^4.8.3",
    "webpack-node-externals": "^1.7.2"
  },
  "private": true,
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "standard --fix",
      "git add"
    ]
  },
  "babel": {
    "presets": [
      ["@babel/preset-env", { "targets": { "node": "8.10" } }]
    ]
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  },
  "standard": {
    "globals": [
      "afterEach",
      "beforeEach",
      "describe",
      "it"
    ]
  }
}
